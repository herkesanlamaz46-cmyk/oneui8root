name: Samsung One UI 8 Root (Optimized)

on:
  workflow_dispatch:
    inputs:
      ui7_link:
        description: 'OneUI 7 Firmware Direct Link (.zip)'
        required: true
        type: string
      ui8_link:
        description: 'OneUI 8 Firmware Direct Link (.zip)'
        required: true
        type: string
      magisk_version:
        description: 'Select Magisk Version (26.3 - 30.6)'
        required: false
        default: '27.0'
        type: choice
        options:
          - '26.3'
          - '26.4'
          - '27.0'
          - '28.0'
          - '29.0'
          - '30.0'
          - '30.6'
      output_selection:
        description: 'Choose Output Files'
        required: false
        default: 'Both (BL + AP)'
        type: choice
        options:
          - 'Both (BL + AP)'
          - 'BL Only'
          - 'AP Only'
      keep_original:
        description: 'Keep Original Boot Image'
        required: false
        default: false
        type: boolean
      debug_mode:
        description: 'Enable Debug Logs'
        required: false
        default: false
        type: boolean

jobs:
  patch-firmware:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      # ============================================================
      # STEP 1: Setup Environment
      # ============================================================
      - name: 1️⃣ Setup Environment
        run: |
          echo "Setting up environment..."
          sudo apt-get update
          sudo apt-get install -y lz4 unzip wget curl tar openssl jq xz-utils file qemu-user-static
          echo "✅ Environment ready"

      # ============================================================
      # STEP 2: Download MagiskBoot (Multiple Sources) ✅
      # ============================================================
      - name: 2️⃣ Download MagiskBoot (x86_64 or ARM64+QEMU)
        run: |
          echo "::group::Step 2: Downloading MagiskBoot"
          
          MAGISK_VER="${{ inputs.magisk_version }}"
          DOWNLOAD_SUCCESS=false
          MAGISKBOOT_CMD="./magiskboot"
          
          # ===== SOURCE 1: Try pre-built magiskboot binary =====
          echo "📥 Source 1: Pre-built magiskboot binary..."
          BINARY_URLS=(
            "https://github.com/5ec1cff/magiskboot/releases/download/v${MAGISK_VER}/magiskboot-linux-x86_64"
            "https://github.com/5ec1cff/magiskboot/releases/download/v27.0/magiskboot-linux-x86_64"
            "https://github.com/5ec1cff/magiskboot/releases/download/v26.4/magiskboot-linux-x86_64"
            "https://github.com/5ec1cff/magiskboot/releases/latest/download/magiskboot-linux-x86_64"
          )
          
          for URL in "${BINARY_URLS[@]}"; do
            echo "Trying: $URL"
            HTTP_CODE=$(curl -sL -w "%{http_code}" -o magiskboot "$URL")
            
            if [ "$HTTP_CODE" = "200" ] && [ -f magiskboot ] && [ -s magiskboot ]; then
              FILE_TYPE=$(file magiskboot)
              echo "File type: $FILE_TYPE"
              
              if echo "$FILE_TYPE" | grep -qE "(ELF|executable)"; then
                chmod +x magiskboot
                echo "✅ Pre-built binary downloaded successfully!"
                DOWNLOAD_SUCCESS=true
                break
              else
                echo "⚠️ Not a valid binary, removing..."
                rm -f magiskboot
              fi
            else
              echo "⚠️ HTTP $HTTP_CODE, trying next..."
              rm -f magiskboot
            fi
          done
          
          # ===== SOURCE 2: Magisk APK Extract =====
          if [ "$DOWNLOAD_SUCCESS" = false ]; then
            echo ""
            echo "📥 Source 2: Magisk APK extraction..."
            APK_URL="https://github.com/topjohnwu/Magisk/releases/download/v${MAGISK_VER}/Magisk-v${MAGISK_VER}.apk"
            
            echo "Downloading APK from: $APK_URL"
            curl -sL --retry 3 -w "\nHTTP Code: %{http_code}\n" -o magisk.apk "$APK_URL"
            
            if [ -f magisk.apk ] && [ -s magisk.apk ]; then
              FILE_TYPE=$(file magisk.apk)
              echo "APK File type: $FILE_TYPE"
              echo "APK Size: $(ls -lh magisk.apk | awk '{print $5}')"
              
              # Verify it's a valid ZIP/APK
              if unzip -t magisk.apk > /dev/null 2>&1; then
                echo "✅ APK is valid ZIP archive"
                
                # List all libmagiskboot files
                echo ""
                echo "Searching for libmagiskboot in APK:"
                unzip -l magisk.apk | grep -i "magiskboot" || echo "⚠️ No magiskboot found!"
                
                # Try x86_64
                echo ""
                echo "Attempting x86_64 extraction..."
                if unzip -o magisk.apk "lib/x86_64/libmagiskboot.so" 2>&1; then
                  if [ -f "lib/x86_64/libmagiskboot.so" ]; then
                    mv lib/x86_64/libmagiskboot.so ./magiskboot
                    chmod +x magiskboot
                    echo "✅ Extracted x86_64 successfully!"
                    DOWNLOAD_SUCCESS=true
                  fi
                fi
                
                # Try arm64 as fallback
                if [ "$DOWNLOAD_SUCCESS" = false ]; then
                  echo ""
                  echo "Attempting arm64 extraction (will use QEMU)..."
                  if unzip -o magisk.apk "lib/arm64-v8a/libmagiskboot.so" 2>&1; then
                    if [ -f "lib/arm64-v8a/libmagiskboot.so" ]; then
                      mv lib/arm64-v8a/libmagiskboot.so ./magiskboot
                      chmod +x magiskboot
                      echo "✅ Extracted arm64 successfully!"
                      MAGISKBOOT_CMD="qemu-aarch64-static ./magiskboot"
                      DOWNLOAD_SUCCESS=true
                    fi
                  fi
                fi
                
                # Try armeabi-v7a as last resort
                if [ "$DOWNLOAD_SUCCESS" = false ]; then
                  echo ""
                  echo "Attempting armeabi-v7a extraction..."
                  if unzip -o magisk.apk "lib/armeabi-v7a/libmagiskboot.so" 2>&1; then
                    if [ -f "lib/armeabi-v7a/libmagiskboot.so" ]; then
                      mv lib/armeabi-v7a/libmagiskboot.so ./magiskboot
                      chmod +x magiskboot
                      echo "✅ Extracted armeabi-v7a successfully!"
                      MAGISKBOOT_CMD="qemu-arm-static ./magiskboot"
                      DOWNLOAD_SUCCESS=true
                    fi
                  fi
                fi
                
                rm -rf lib
              else
                echo "❌ APK is not a valid ZIP file!"
              fi
              rm -f magisk.apk
            else
              echo "❌ APK download failed!"
            fi
          fi
          
          # ===== SOURCE 3: Latest Magisk APK =====
          if [ "$DOWNLOAD_SUCCESS" = false ]; then
            echo ""
            echo "📥 Source 3: Latest Magisk APK fallback..."
            curl -sL -o magisk.apk "https://github.com/topjohnwu/Magisk/releases/latest/download/Magisk.apk"
            
            if [ -f magisk.apk ] && [ -s magisk.apk ] && unzip -t magisk.apk > /dev/null 2>&1; then
              echo "✅ Latest APK valid"
              unzip -l magisk.apk | grep -i "magiskboot" || true
              
              if unzip -o magisk.apk "lib/arm64-v8a/libmagiskboot.so" 2>&1; then
                if [ -f "lib/arm64-v8a/libmagiskboot.so" ]; then
                  mv lib/arm64-v8a/libmagiskboot.so ./magiskboot
                  chmod +x magiskboot
                  MAGISKBOOT_CMD="qemu-aarch64-static ./magiskboot"
                  DOWNLOAD_SUCCESS=true
                fi
                rm -rf lib
              fi
              rm -f magisk.apk
            fi
          fi
          
          # ===== FINAL CHECK =====
          echo ""
          echo "========================================="
          if [ "$DOWNLOAD_SUCCESS" = true ]; then
            echo "✅ MagiskBoot obtained successfully!"
            echo "Command: $MAGISKBOOT_CMD"
            echo "File info: $(file magiskboot)"
            echo "File size: $(ls -lh magiskboot | awk '{print $5}')"
            
            # Save command to env
            echo "MAGISKBOOT_CMD=$MAGISKBOOT_CMD" >> $GITHUB_ENV
            
            # Verify
            if $MAGISKBOOT_CMD --help > /dev/null 2>&1; then
              echo "✅ MagiskBoot verification passed!"
            else
              echo "⚠️ Warning: Help check skipped, proceeding..."
            fi
          else
            echo "❌ Critical: Could not obtain magiskboot from any source!"
            echo "Please check logs above for details."
            exit 1
          fi
          echo "========================================="
          echo "::endgroup::"

      # ============================================================
      # STEP 3: Download Firmware
      # ============================================================
      - name: 3️⃣ Download Firmware (Large Files)
        run: |
          echo "::group::Step 3: Downloading Firmware"
          echo "⏳ Starting large file downloads..."
          
          echo "📥 Downloading OneUI 7..."
          curl -L --progress-bar "${{ inputs.ui7_link }}" -o ui7.zip || { 
            echo "❌ Error: UI7 Download Failed!"
            exit 1
          }
          echo "✅ OneUI 7 complete"
          
          echo "📥 Downloading OneUI 8..."
          curl -L --progress-bar "${{ inputs.ui8_link }}" -o ui8.zip || { 
            echo "❌ Error: UI8 Download Failed!"
            exit 1
          }
          echo "✅ OneUI 8 complete"
          
          echo "📊 Download Summary:"
          ls -lh ui7.zip ui8.zip
          
          mkdir -p ui7_folder ui8_folder
          echo "📦 Extracting UI7..."
          unzip -q -o ui7.zip -d ui7_folder
          echo "📦 Extracting UI8..."
          unzip -q -o ui8.zip -d ui8_folder
          echo "✅ Extraction complete"
          echo "::endgroup::"

      # ============================================================
      # STEP 4: Bit Version Check
      # ============================================================
      - name: 4️⃣ Bit Version Check
        id: bit_check
        continue-on-error: true
        run: |
          echo "::group::Step 4: Bit Version Check"
          
          extract_bit() {
            local folder=$1
            AP_FILE=$(find "$folder" -name "AP_*.tar.md5" -o -name "AP_*.tar" | head -n1)
            if [ -z "$AP_FILE" ]; then
              AP_FILE=$(find "$folder" -name "BL_*.tar.md5" -o -name "BL_*.tar" | head -n1)
            fi
            if [ -z "$AP_FILE" ]; then
              echo "UNKNOWN"
              return 1
            fi
            
            fw_name=$(basename "$AP_FILE")
            bit_ver=$(echo "$fw_name" | grep -oP '(XXU|XSU|XXS|OXU)\K[0-9]' | head -n1)
            
            if [ -z "$bit_ver" ]; then
              echo "UNKNOWN"
              return 1
            fi
            echo "$bit_ver"
          }
          
          BIT_UI7=$(extract_bit "ui7_folder")
          BIT_UI8=$(extract_bit "ui8_folder")
          
          echo "OneUI 7 Bit: $BIT_UI7"
          echo "OneUI 8 Bit: $BIT_UI8"
          
          if [ "$BIT_UI7" != "$BIT_UI8" ] && [ "$BIT_UI7" != "UNKNOWN" ] && [ "$BIT_UI8" != "UNKNOWN" ]; then
            echo "⚠️ WARNING: Bit Version Mismatch!"
          else
            echo "✅ Bit versions match"
          fi
          
          echo "bit_version=$BIT_UI8" >> $GITHUB_OUTPUT
          echo "::endgroup::"

      # ============================================================
      # STEP 5: Patch AP (Using MAGISKBOOT_CMD)
      # ============================================================
      - name: 5️⃣ Patch AP (Magisk Root)
        id: patch_ap
        run: |
          echo "::group::Step 5: Patching AP with Magisk"
          mkdir -p output
          
          # Load magiskboot command from env
          MAGISKBOOT_CMD="${MAGISKBOOT_CMD:-./magiskboot}"
          echo "Using magiskboot command: $MAGISKBOOT_CMD"
          
          # Verify magiskboot exists
          if [ ! -f magiskboot ]; then
            echo "❌ Error: magiskboot file not found!"
            ls -la
            exit 1
          fi
          
          AP_FILE=$(find ui8_folder -name "AP_*.tar.md5" | head -n 1)
          if [ -z "$AP_FILE" ]; then
            AP_FILE=$(find ui8_folder -name "AP_*.tar" | head -n 1)
          fi
          
          if [ -z "$AP_FILE" ]; then
            echo "❌ Error: AP file not found!"
            ls -la ui8_folder/
            exit 1
          fi
          
          echo "Found AP: $AP_FILE"
          echo "File size: $(ls -lh "$AP_FILE" | awk '{print $5}')"
          
          cp "$AP_FILE" AP_work.tar
          
          if [[ "$AP_FILE" == *.md5 ]]; then
            echo "Removing MD5 checksum line..."
            head -c -56 AP_work.tar > AP_clean.tar 2>/dev/null || {
              cp AP_work.tar AP_clean.tar
              echo "Warning: Could not remove MD5, continuing..."
            }
            mv AP_clean.tar AP_work.tar
          fi
          
          echo "Extracting tar..."
          tar -xf AP_work.tar --ignore-zeros 2>/dev/null || tar -xf AP_work.tar
          
          BOOT_IMG=""
          if [ -f "init_boot.img" ]; then
            BOOT_IMG="init_boot.img"
            echo "Found: init_boot.img"
          elif [ -f "boot.img" ]; then
            BOOT_IMG="boot.img"
            echo "Found: boot.img"
          elif [ -f "boot.img.lz4" ]; then
            echo "Found: boot.img.lz4 (Decompressing...)"
            lz4 -d boot.img.lz4 boot.img
            BOOT_IMG="boot.img"
          else
            echo "❌ No boot image found!"
            find . -maxdepth 1 -name "*.img*" -ls
            exit 1
          fi
          
          if [ "${{ inputs.keep_original }}" == "true" ]; then
            cp "$BOOT_IMG" "output/Original_$BOOT_IMG"
          fi
          
          echo "Patching $BOOT_IMG..."
          $MAGISKBOOT_CMD patch "$BOOT_IMG"
          
          if [ -f "${BOOT_IMG}.patched" ]; then
            mv "${BOOT_IMG}.patched" "$BOOT_IMG"
            echo "Using .patched file"
          elif [ -f "${BOOT_IMG}.magisk" ]; then
            mv "${BOOT_IMG}.magisk" "$BOOT_IMG"
            echo "Using .magisk file"
          else
            echo "Warning: No patched file, using original"
          fi
          
          echo "Repacking AP_Patched.tar..."
          tar -cf AP_Patched.tar --format=ustar "$BOOT_IMG"
          MD5_HASH=$(md5sum AP_Patched.tar | awk '{print $1}')
          echo "$MD5_HASH  AP_Patched.tar" >> AP_Patched.tar
          mv AP_Patched.tar output/AP_Patched.tar
          
          echo "✅ AP Patching Complete"
          echo "::endgroup::"

      # ============================================================
      # STEP 6: Prepare BL
      # ============================================================
      - name: 6️⃣ Prepare BL
        run: |
          echo "::group::Step 6: Preparing BL"
          mkdir -p output
          
          BL_FILE=$(find ui8_folder -name "BL_*.tar.md5" | head -n 1)
          if [ -z "$BL_FILE" ]; then
            BL_FILE=$(find ui8_folder -name "BL_*.tar" | head -n 1)
          fi
          
          if [ -z "$BL_FILE" ]; then
            echo "Warning: BL file not found"
          else
            cp "$BL_FILE" output/BL_Patched.tar
            echo "BL copied"
          fi
          echo "::endgroup::"

      # ============================================================
      # STEP 7: Generate Device Info TXT
      # ============================================================
      - name: 7️⃣ Generate Device Info TXT
        run: |
          echo "::group::Step 7: Generating Device Info"
          BIT_VER="${{ steps.bit_check.outputs.bit_version }}"
          cat > output/device_info.txt << EOF
          ========================================
          Samsung Firmware Patch Information
          ========================================
          Date: $(date)
          Workflow Run ID: ${{ github.run_id }}
          
          Firmware Sources:
          - OneUI 7 Link: ${{ inputs.ui7_link }}
          - OneUI 8 Link: ${{ inputs.ui8_link }}
          
          Configuration:
          - Magisk Version: ${{ inputs.magisk_version }}
          - Output Selection: ${{ inputs.output_selection }}
          - Keep Original Boot: ${{ inputs.keep_original }}
          
          🔐 Security:
          - Bit Version: $BIT_VER
          - Ramdisk: Auto-Handled
          
          Patching Status:
          - AP: Patched with MagiskBoot
          - BL: Prepared from UI8
          ========================================
          ⚠️ Flash at your own risk!
          ========================================
          EOF
          echo "::endgroup::"

      # ============================================================
      # STEP 8: Upload Artifacts
      # ============================================================
      - name: 8️⃣ Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: Samsung-Rooted-Files
          path: output/
          retention-days: 5
